<%= form_with(model: beer_club, local: true) do |form| %>
  <% if beer_club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(beer_club.errors.count, "error") %> prohibited this beer_club from being saved:</h2>

      <ul>
      <% beer_club.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="container">
    <div class="row">
    <div class="col-sm-2"><%= form.label :name %></div>
    <div class="col-md-10"><%= form.text_field :name, class:"form-control" %></div>
    </div>

    <div class="row">
    <div class="col-sm-2"><%= form.label :founded %></div>
    <div class="col"><%= form.number_field :founded, class:"form-control"%></div>
    </div>

    <div class="row">
    <div class="col-sm-2"><%= form.label :city %></div>
    <div class="col"><%= form.text_field :city, class:"form-control"%></div>
    </div>
  </div>

  <div class="actions">
    <%= form.submit class:"btn btn-primary"%>
  </div>
<% end %>
